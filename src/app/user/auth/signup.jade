form.form-signin(ng-submit="vm.signup()",name="signup")
  h2.form-signin-heading Create an account
  div.form-group(ng-class="{'has-error': signup.email.$dirty && signup.email.$invalid}")
    label.sr-only(for="email") Email
    input.form-control#email(type="email",name="email", ng-model="vm.username", required, autofocus, placeholder="Email")
  div.form-group(ng-class="{'has-error': signup.password.$dirty && signup.password.$invalid}")
    label.sr-only(for="password") Password
    input.form-control#password(type="password",name="password", ng-model="vm.password", required, placeholder="Password")
  div.form-group
    label
      input(type="checkbox",value="ok",required)
      | Yes, I promise to be funny and useful

  button.btn.btn-lg.btn-warning.btn-block(type="submit", ng-disabled="signup.$invalid||vm.saving") Create

  p Already have an account?
    a.btn.btn-sm.btn-default(ui-sref="login") Login!
