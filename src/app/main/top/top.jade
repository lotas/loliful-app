float-menu(single="true")

loading(ng-if="vm.$loading")

div.page-top
  .row
    .col-sm-6
      ul#secondary-menu.nav.nav-pills.activity.nav-justified
        li(role="presentation", ng-class="{active: vm.period == 'day' }")
          a(ui-sref="top({period: 'day'})")
            | За сегодня
        li(role="presentation", ng-class="{active: vm.period == 'week' }")
          a(ui-sref="top({period: 'week'})")
            | За неделю
        li(role="presentation", ng-class="{active: vm.period == 'month' }")
          a(ui-sref="top({period: 'month'})")
            | За месяц
        li(role="presentation", ng-class="{active: vm.period == 'all' }")
          a(ui-sref="top({period: 'all'})")
            | За всё время


  div.top(ng-if="vm.jokes",infinite-scroll="screen.isPhone && vm.loadMore()" infinite-scroll-distance="3")
    joke-list-item(joke="item", ng-repeat="item in vm.jokes track by $index")

  button.load-more.btn.btn-block.btn-lg(ng-click="vm.loadMore()", ng-hide="!vm.$hasMore || screen.isPhone") Show more

  br

  div.container-fluid.align-center(ng-if="vm.$empty")
    p.vOfs Похоже что никто не старался быть смешным.

    .empty-card.vOfs
      img(src="/assets/img/empty-card.png")

    p Может быть у тебя получится?

    .vOfs
      a.btn.btn-primary.uppercase(ui-sref="fresh") Пошутить
