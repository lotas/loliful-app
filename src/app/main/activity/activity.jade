float-menu(single="true")

ul#secondary-menu.nav.nav-pills.activity.nav-justified
  li(role="presentation", ui-sref-active="active")
    a(ui-sref="activity({type: 'saves'})")
      | Saved
      span.hidden-xs &nbsp; for later
  li(role="presentation", ui-sref-active="active")
    a(ui-sref="activity({type: 'likes'})")
      | Liked
  li(role="presentation", ui-sref-active="active")
    a(ui-sref="activity({type: 'nails'})")
      | My intros
  li(role="presentation", ui-sref-active="active")
    a(ui-sref="activity({type: 'hammers'})")
      | My outros


section
  loading(ng-if="vm.$loading")

  div.activities(infinite-scroll="screen.isPhone && vm.loadMore()" infinite-scroll-distance="3")
    div.my-nails(ng-if="vm.items && vm.type=='nails'")
      nail-list-item(nail="item", ng-repeat="item in vm.items track by $index")
    div.my-hammers(ng-if="vm.items && vm.type=='hammers'")
      hammer-activity-item(hammer="item", ng-repeat="item in vm.items track by $index")
    div(ng-if="vm.items && (vm.type=='saves' || vm.type=='likes')")
      div(ng-repeat="item in vm.items track by $index")
        nail-list-item(ng-if="item.$nail", nail="item.$nail")
        hammer-list-item(ng-if="item.$hammer", hammer="item.$hammer")

  button.load-more.btn.btn-block.btn-lg(ng-click="vm.loadMore()", ng-hide="!vm.$hasMore || screen.isPhone") Show more

  br


  div.container-fluid.align-center(ng-if="vm.$empty")
    p.vOfs {{ vm.typeName }} you create will appear here

    .empty-card.vOfs
      img(src="/assets/img/empty-card.png")

    p In <b>Fresh</b> pick the <b>Intro</b> you like and add your <b>Outro</b> to it
    p If you don't feel like joking just yet, start with a <b>Save for later</b> or a goold old <b>Like</b>

    .vOfs
      a.btn.btn-primary.uppercase(ui-sref="fresh") Take me to intros
